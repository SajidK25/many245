services:
  web:
    build: .
    restart: always
    ports:
      - "8001:8001"
    environment:
      - FLASK_DEBUG=1
    volumes: 
      - /var/run/docker.sock:/var/run/docker.sock
    # command: ["sh", "-c", "gunicorn -w 4 -b 0.0.0.0:8001 app:app"]
